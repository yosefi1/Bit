# פריסה ובדיקת תשלום בביט

מדריך זה מסביר איך להעלות את האתר ל-Vercel ולבדוק את תהליך התשלום בביט.
ביט **לא ניתן לבדיקה מלאה על localhost** — צריך אתר באינטרנט (HTTPS) וטלפון עם אפליקציית ביט.

---

## שלב 1 — הכנת מסד נתונים (PostgreSQL)

1. צור מסד נתונים ב-**Supabase**, **Neon** או **Vercel Postgres**.
2. העתק שני connection strings:
   - **Pooled** (פורט 6543) → `DATABASE_URL`
   - **Direct** (פורט 5432) → `DIRECT_URL`

3. ב-`prisma/schema.prisma` החלף את ה-datasource:

```prisma
datasource db {
  provider  = "postgresql"
  url       = env("DATABASE_URL")
  directUrl = env("DIRECT_URL")
}
```

4. הרץ מקומית (פעם אחת):

```bash
npx prisma db push
npm run db:seed
```

---

## שלב 2 — העלאה ל-Vercel

1. דחוף את הקוד ל-GitHub.
2. ב-[vercel.com](https://vercel.com) → **Add New Project** → בחר את הריפו.
3. הגדר **Environment Variables**:

| משתנה | ערך |
|--------|-----|
| `DATABASE_URL` | connection string (pooled) |
| `DIRECT_URL` | connection string (direct) |
| `NEXTAUTH_SECRET` | מחרוזת אקראית: `openssl rand -base64 32` |
| `NEXTAUTH_URL` | `https://your-app.vercel.app` |
| `BIT_PHONE` | **מספר הטלפון שלך בביט** (למשל `0501234567`) |
| `BIT_RECIPIENT_NAME` | השם שמופיע בביט |
| `SEED_ADMIN_USERNAME` | `admin` |
| `SEED_ADMIN_PASSWORD` | סיסמה חזקה |

4. **Deploy**.

5. אחרי הפריסה הראשונה — הרץ seed על מסד ה-production (מהמחשב שלך):

```bash
set DATABASE_URL=postgresql://...
set DIRECT_URL=postgresql://...
npm run db:seed
```

---

## שלב 3 — הגדרות באתר (מנהל)

1. היכנס כ-**מנהל** (`admin` / הסיסמה שהגדרת).
2. עבור ל-**הגדרות**:
   - **מחיר חשמל**: ודא שמופיע **64.32 אג׳ / קו״ח** (רק מנהל יכול לשנות).
   - **ביט**: הזן את **מספר הטלפון האמיתי** שלך בביט, שם הנמען, והוראות לדיירים.
3. צור **מחזור חיוב** פתוח (מחזורי חיוב → חדש).
4. ודא שלדייר יש דירה ומשתמש פעיל.

---

## שלב 4 — בדיקת תהליך התשלום (מטלפון)

### כמנהל
1. אשר דיווח קריאה של דייר (דיווחים → פתיחה → **אישור**).
2. זה יוצר רשומת תשלום בסטטוס **ממתין לתשלום**.

### כדייר (מהטלפון — חשוב!)
1. התחבר כמשתמש דייר (למשל `boaz` / `tenant123` אחרי seed).
2. פתח את **הדירה שלי** → **תשלום בביט** (או מדיווח מאושר).
3. ודא שמופיעים:
   - הסכום לתשלום
   - מספר הטלפון שלך
   - שם הנמען
   - שם הדירה כהערה
4. לחץ **פתיחת ביט / חיוג** — ייפתח חיוג או תוכל להעתיק את המספר לביט.
5. **שלח בביט** את הסכום המדויק למספר שהגדרת.
6. חזור לאתר → **שלחתי את התשלום**.

### כמנהל — אימות
1. בדוק בחשבון הבנק/ביט שהכסף הגיע.
2. בדיווח → **סימון כשולם** (או עדכון סטטוס תשלום).

---

## שלב 5 — אחסון תמונות (מומלץ ל-production)

ב-Vercel הקבצים ב-`/public/uploads` **לא נשמרים** בין הפעלות.

1. התקן: `npm install @vercel/blob`
2. ב-Vercel → Storage → Blob → צור store → העתק `BLOB_READ_WRITE_TOKEN`
3. הגדר `STORAGE_DRIVER=vercel-blob`
4. בטל הערה בקוד ב-`src/lib/storage.ts` (סעיף vercel-blob)

---

## מה ביט **לא** תומך בו כרגע

- אין קישור deep-link שממלא סכום אוטומטית בדפדפן.
- על **מובייל** — הכפתור פותח חיוג; פתח את **אפליקציית ביט** ידנית והעתק את הסכום והמספר מהמסך.
- אם ביט יפרסמו deep-link רשמי — רק `src/lib/bit.ts` צריך עדכון.

---

## בדיקה מהירה בלי פריסה (מוגבל)

| מה | localhost | production |
|----|-----------|------------|
| התחברות, דיווחים, OCR | ✅ | ✅ |
| תצוגת מסך תשלום ביט | ✅ | ✅ |
| העתקת סכום/טלפון | ✅ | ✅ |
| שליחת כסף אמיתי בביט | ❌ | ✅ (מהטלפון) |

---

## סיכום צ'ק-ליסט

- [ ] מסד PostgreSQL + `db push` + `db:seed`
- [ ] פריסה ל-Vercel + `NEXTAUTH_URL` נכון
- [ ] הגדרות: מחיר 64.32 אג׳, טלפון ביט אמיתי
- [ ] מחזור חיוב פתוח
- [ ] דיווח מאושר → מסך תשלום
- [ ] בדיקה **מהטלפון** עם אפליקציית ביט
- [ ] (מומלץ) Vercel Blob לתמונות
